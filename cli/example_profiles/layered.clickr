profile "Layered Profile"

config {
    default_layer = "base"
}

layer "base" {
    # This is a comment
    hold(esc) = [t, i, m]
    _capslock = layer("apps")
}

layer "apps" {
    g     = open_app("Google Chrome")
    t     = open_app("Terminal")
    ^capslock = layer("apps")
}
